VITE_API_BASE_URL=/api/v1
VITE_API_PROXY_TARGET=http://localhost:8090

# Show every feature module (Pharmacy, Pub/Restaurant) regardless of business
# type. Set to false to restore business-type gating per the README.
VITE_SHOW_ALL_MODULES=true
